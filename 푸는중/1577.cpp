#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int n, m, k;
vector<vector<int>> r;


int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    cin >> n >> m >> k;


    return 0;
}
